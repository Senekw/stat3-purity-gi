# Data is large and re-downloadable; provenance is committed instead.
data/raw/
# ...but the GEO tumour channel list IS provenance: analysis_plan.md requires the
# 129-channel count to be asserted against a committed artefact rather than
# re-fetched from GEO at analysis time.
!data/raw/GSE178341_geo_tumour_channels.txt
data/tcga/
data/geo/
data/manual/
data/cache/
!data/cache/provenance.txt

# renv library is machine-specific; renv.lock IS committed.
renv/library/
renv/local/
renv/staging/
renv/python/
renv/sandbox/

.Rhistory
.RData
.Rproj.user/
.DS_Store
renv/library-local/
MANIFEST.txt
*.rds
!data/**/*.rds
data/
renv/library-local/
MANIFEST.txt
*.rds
!data/**/*.rds
data/
null_ckpt_*.rds
figures/*.csv
Rplots.pdf
data/validation/RPPA/*.csv
